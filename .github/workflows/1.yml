name: Event Filters
on: 
  push:
    tags:
    - "v*"
    - "rel*
    tags-ignore:
    - "t*"
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: test step
      run: |
         pwd
         echo "${{ github.ref }}"

