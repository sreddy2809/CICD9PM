name: Event Filters
on: 
  push:
    branches:
    - main
    tags:
    - "v*"
   
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: test step
      run: |
         pwd
         echo "${{ github.ref }}"
         
 


git push origin main
git push origin v1.1 


       