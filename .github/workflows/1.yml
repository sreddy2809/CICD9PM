name: Event Filters
on: 
  push:
    branches:
    - main
    - UAT

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: test step
      run: |
         pwd
         echo "${{ github.ref }}"
